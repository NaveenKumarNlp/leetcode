class Solution {
    public int[] shuffle(int[] nums, int n) {
        int []temp=new int[n*2];
        int j=0;
        for(int i=0;i<n;i++){
            temp[j++]=nums[i];
            temp[j++]=nums[n+i];
        }
        return temp;
    }
}
