class Solution {
    public int mirrorDistance(int n) {
        int sum=0,temp=n;
        while(n>0){
            int rem=n%10;
            sum=sum*10+rem;
            n/=10;
        }
        return Math.abs(temp-sum);
    }
}
