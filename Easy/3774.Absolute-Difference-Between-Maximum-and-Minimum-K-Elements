class Solution {
    public int absDifference(int[] nums, int k) {
        Arrays.sort(nums);
        int first=0,last=0;
        for(int i=0;i<nums.length && i<k;i++){
            first+=nums[i];
            last+=nums[nums.length-i-1];
        }
        return Math.abs(first-last);
    }
}
