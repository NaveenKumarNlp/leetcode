class Solution {
    public int minMoves(int[] nums) {
        int max=-1;
        for(int i:nums){
            max=(max<i)?i:max;
        }
        int step=0;
        for(int i:nums){
            step+=(max-i);
        }
        return step;
    }
}
