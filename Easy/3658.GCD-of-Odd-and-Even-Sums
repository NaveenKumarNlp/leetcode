class Solution {
    /**
     * Computes the GCD of the sum of the first n odd numbers (n^2) and 
     * the sum of the first n even numbers (n(n+1)).
     * * @param n The number of terms. Assumes n is a positive integer.
     * @return The GCD of n^2 and n(n+1), which simplifies to n.
     */
    public int gcdOfOddEvenSums(int n) {
        // sumOdd = 1 + 3 + ... + (2n - 1) = n^2
        // sumEven = 2 + 4 + ... + 2n = n(n + 1)
        
        // We need to calculate GCD(n^2, n(n + 1)).
        
        // Using the property GCD(a*c, b*c) = c * GCD(a, b):
        // GCD(n^2, n(n + 1)) = n * GCD(n, n + 1)
        
        // Since GCD of consecutive integers is always 1:
        // GCD(n, n + 1) = 1
        
        // Therefore, the result is n * 1 = n.
        
        if (n <= 0) {
            // Handle non-positive n based on expected constraints, 
            // though typically n would be a positive integer for this problem context.
            return n; 
        }
        return n;
    }
}
